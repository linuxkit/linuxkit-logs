I believe from Rolf's link that in the linuxkit kernel container images it is always `/kernel` today (that snippet installs to `/out` but then the final image is `FROM scratch` with a `COPY --from=kernel-build /out/* /`).

