This ended up being confusing, but I think I got it. All of the above use `/boot/kernel` for `tar` output, but `/kernel` for the `tar-initrd` output. We `tar-initrd` for all of the above, so need to switch it.