Signed-off-by: Avi Deitcher <avi@deitcher.net>

<!--
Please make sure you've read and understood our contributing guidelines;
https://github.com/linuxkit/linuxkit/blob/master/CONTRIBUTING.md

** Make sure all your commits include a signature generated with `git commit -s` **

For additional information on our contributing process, read our contributing
guide https://docs.docker.com/opensource/code/

If this is a bug fix, make sure your description includes "fixes #xxxx", or
"closes #xxxx"

Please provide the following information:
-->

fixes #3340 

**- What I did**

Changes the 7 `mkimage-*` utilities that call `KERNEL=$(find -name kernel)` or similar to `KERNEL=$(find -type f -name kernel)`

**- How I did it**

With `vi` (because I still like `vi`)

**- How to verify it**

Build and deploy. And CI

**- Description for the changelog**
<!--
Write a short (one line) summary that describes the changes in this
pull request for inclusion in the changelog:
-->
Fix bug wherein dir named `kernel` in image breaks builder

Notes:

* This is not ready. I did not updated hashes in the `output.go` file yet because:
* I want feedback from other maintainers on if this is sufficient or if they have other ideas. Once we are agreed, I will updated the hashes.