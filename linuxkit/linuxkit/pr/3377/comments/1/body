That's why one of the dependencies (`sshkeys`) requires `blowfish` which is in `crypto`. I tried to add `blowfish` to the vendor.conf but it said it was a duplicate of `golang.org/x/crypto`. Maybe there is a trick I don't know about (no expert with vndr :sweat_smile:)