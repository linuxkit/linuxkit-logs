The `onboot` is a symlink to `/run/log/onboot` and is created here: https://github.com/linuxkit/linuxkit/blob/master/pkg/init/cmd/service/runc.go#L162

Maybe we can make this conditional on the logger being registered?