Oh, I remember now: I think it has to be writing to `/run/log/onboot` otherwise any `onboot` logs which happen before a mount of the persistent disk might be unavailable if `/var/log` gets overmounted.