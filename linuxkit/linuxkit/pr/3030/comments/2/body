An earlier version of this PR attempted to remove the `unix.Reboot()` call from rc.shutdown, however, it's still needed to handle the ctl-alt-del case.  This updated version simply uses an argument to `rc.shutdown` that won't trigger any `reboot(2)` actions from `rc.shutdown` and instead defers the decision to `init`.