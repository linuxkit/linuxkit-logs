It's a little odd that it fails on the `init` build. Space requirements aren't/shouldn't be that high. In any case, this can be "fixed" by building/pushing/signing the init package so that the CI is not doing it. I can do that after the kernel updates I'm currently running