It's also pulling everything in `pkg/*` it isn't building, which will be taking space in `/var/lib/docker`, quite why building a single package then pushes it over the edge I'm unsure. Maybe it's right on the edge most of the time?