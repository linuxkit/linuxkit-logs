yes, the `-amd64` image is on hub but not in the manifest list. I think this is when the trust update timed out or something (I can't recall the exact error message). Anyway, just `forcepush_4.14.x` should fix this and then I hit rebuild on the CI