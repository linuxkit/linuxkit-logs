fixes https://github.com/linuxkit/linuxkit/issues/1863

**- What I did**

Fixed the `Makefile` for Windows by:
* setting `GOOS` to `windows` on Windows
* appending `.exe` to binaries on Windows

**- How to verify it**

Running `make` on Windows now succeeds, although it still requires a unixy shell (cygwin, msys, …).
