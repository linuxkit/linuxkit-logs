@ijc thanks for the review. I think I have addressed all the comments:
- `sources` -> `extra-sources`
- Always use the build context. Makes the code a little simpler
- Propagate errors from the build context `Copy()` function and then use an `errgroup`
- plus the minor bits and pieces