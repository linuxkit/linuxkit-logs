Thanks for the patience. I've rebuild and pushed the `init` package for all arches. Could you now push a new commit again updating the `init` package to:
```
linuxkit/init:eca81db9b072eb431c057bab77797e169295fa1e
```

This now should not have merge conflicts and also the right meta-data