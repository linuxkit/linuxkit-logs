Signed-off-by: justinbarrick <jbarrick@cloudflare.com>

**- What I did**

Added restart and stop commands to the `service` command to restart and stop services.

**- How to verify it**

Run `service restart someservice` and `service stop someservice`.

**- Description for the changelog**

Added restart and stop commands to the `service` command to restart and stop services.