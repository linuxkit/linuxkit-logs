This does *not* switch to `alpine:3.9` base for grub. There was a build problem with coreos grub and `alpine:3.9`. It _does_ do so for the dependent images, `tools/mkimage-*-efi/`. If I can get the grub build to work on `alpine:3.9`, great, but let's not hold the incremental improvements up.