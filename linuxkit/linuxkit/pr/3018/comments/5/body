@rn The CI is failing due to lack of support of creack/goselect for s390x. Should it be implemented in the package in order to merge the PR? Or is it okay if it does not compile for s390x. I'm guessing the first one but not sure. 