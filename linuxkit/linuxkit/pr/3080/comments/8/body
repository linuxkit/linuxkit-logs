Ah yes, I remember the error now; the build didn't even reach --rewrite-arch; it failed on line 26:

```
cp /home/builder/packages/$(uname -m)/wireguard-tools-[0-9]*.apk /mirror/$(uname -m)
```

Since /home/builder/packages/armv7l didn't exist.