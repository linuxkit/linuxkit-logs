> Couldn't find anything
  
Yeah, I had that issue a while ago. Ended up writing a very very basic `yql` like `jql`, but it was a standalone command, not lib, and never fully implemented the `jql` syntax.