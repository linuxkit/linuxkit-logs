Sorry, just having some issues testing this as there seems to be some weirdness sharing the `binfmt` filesystem.

The reason I want to do some testing is that there are patches in the Alpine qemu binaries for signal handling in Go, which weren't in upstream previously.