Ok, it seems to run at least some Go binaries (eg I compiled and run linuxkit for arm64) so I think its ok. However, the way this is being run is not really working, when I bind mount the `binfmt` filesystem it is empty, so I am going to do a PR to change how it works slightly.