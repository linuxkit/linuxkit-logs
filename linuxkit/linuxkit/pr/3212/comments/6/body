Rebased and force pushed, will merge with the existing approvals once CI is green.

I dropped the containerd maintainers a line re the new need to bind `/var/lib/containerd` just in case it wasn't expected. We shouldn't block this PR if it wasn't though and should instead revert the relevant commit whenever we pickup a fixed v1.2.x.