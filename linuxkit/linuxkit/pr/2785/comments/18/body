> we need a service like rngd to read entropy from /dev/hwrng and feed it into the entropy pool of /dev/random

Uh, isn't that precisely what I asked?

> rngd's job was to read from a hardware random device (like /dev/hwrng) and feed /dev/random.