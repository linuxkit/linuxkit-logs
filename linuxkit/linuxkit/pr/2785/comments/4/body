In that case, I don't get how this PR helps. Doesn't this change `pkg/rngd` which runs _inside_ the guest and thus has no knowledge? Would we not need to change how `lkt run` calls hyperkit or qemu command-line?