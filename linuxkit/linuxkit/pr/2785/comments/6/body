Ah, so you are not fixing that issue, but rather intelligently checking if `hwrng` exists in the guest? But again, won't it exist in the guest and be (possibly) fake anyways, because it is backed by `urandom` in the underlying host?