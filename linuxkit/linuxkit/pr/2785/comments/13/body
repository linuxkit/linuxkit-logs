> you probably want to mix them into host entropy and pass that

Didn't understand this. What is mapped to what? Does `hwrng` exist on the guest and if so, mapped to where? If it is to `random` (or `urandom`), then it is not a true `hwrng`.

Or are you saying `lkt run` isn't meant for production use, ever, so it doesn't matter?