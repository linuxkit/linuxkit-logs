@deitch Correct. With `virtio-rng`, we fake a RNG device in the guest but it actually backed by `/dev/urandom`, the latter doesn't block if it does not generate enough entropy, eg, output the same number repeatedly. The essential is we need to make sure guest realize this.