We should not make this code arm64 specific - `/dev/hwrng` is generic and will cover a tpm rng on amd64 platform too, which I have been meaning to add support for, so we should move it to generic code that is used when this device exists.