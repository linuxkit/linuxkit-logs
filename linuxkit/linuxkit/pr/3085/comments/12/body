 can you check locally? Picking a random failing test, in `test/cases/020_kernel/009_config_4.17.x` I've done `linuxkit build test.yml` followed by `linuxkit run qemu test` on a mac. make sure you have the `linuxkit/init` hash adjusted and have a local `linuxkit pkg build .` in `./pkg/init`