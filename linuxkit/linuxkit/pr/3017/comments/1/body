I only went so far as to check that `sed -E` was documented in `sed(1)` on mac. It's part of POSIX and our usage here is fairly simple and something I've used on Mac in the past, so I think it should be ok.

But I'll check for sure!