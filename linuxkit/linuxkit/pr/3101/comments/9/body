Updated to address the most recent set of comments.

In further testing, I noticed that some of the scripts that depend on structure offsets can get confused if the kernel is built with CONFIG_GCC_PLUGIN_RANDSTRUCT.  Building a kernel without this enabled does allow the trace tools to work properly. 