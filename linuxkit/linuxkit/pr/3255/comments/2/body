Yeah, was considering making the timestamp configurable. But rather than via the commandline, we should probably support [this proposal](https://reproducible-builds.org/docs/source-date-epoch/) and make it configurable via the environment variable `SOURCE_DATE_EPOCH`. Just want to get the basics in place and then iterate over refinements...