It looks like the firmware had already been updated since v0.7 (which I was using) and the version in master will already support the 3B+
This still bumps to a later release tag on the firmware repo which seems a valid thing to do, but my original reasoning is no longer relevant.